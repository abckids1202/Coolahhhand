import { describe, expect, it } from "vitest";
import { InteractionRuntime } from "./interactionStateMachine";
import type { StableTrackedHand, TwoHandEffectAnchor } from "../tracking/tracking.types";
const hand=(id:string):StableTrackedHand=>({id,anatomicalSide:"Unknown",screenSide:id==="a"?"Left":"Right",landmarks:[],palmNormalized:{x:0,y:0,z:0},palmScreen:{x:id==="a"?100:500,y:300},palmWorld:{x:id==="a"?-.5:.5,y:0,z:0},pinchPointWorld:{x:0,y:0,z:0},velocityWorld:{x:0,y:0,z:0},speed:0,openness:.8,pinchStrength:0,estimatedDepth:0,palmFacingScore:.6,rawGesture:"open-palm",stableGesture:"open-palm",gestureConfidence:.8,trackingConfidence:.9,firstSeenAt:0,lastSeenAt:0});
const anchor=(closing=0,opening=0):TwoHandEffectAnchor=>({leftHandId:"a",rightHandId:"b",leftPalm:{x:-.5,y:0,z:0},rightPalm:{x:.5,y:0,z:0},midpoint:{x:0,y:0,z:0},smoothedMidpoint:{x:0,y:0,z:0},direction:{x:1,y:0,z:0},distance:1,smoothedDistance:1,normalizedDistance:.4,angle:0,distanceVelocity:opening-closing,closingSpeed:closing,openingSpeed:opening,radius:80,targetRadius:80,facingConfidence:.6,stabilityConfidence:.8,overallConfidence:.7});
describe("orb interaction runtime",()=>{it("keeps timers persistent and forms automatically",()=>{const r=new InteractionRuntime();r.update([hand("a"),hand("b")],anchor(),0);expect(r.state).toBe("two-hand-candidate");r.update([hand("a"),hand("b")],anchor(),200);expect(r.state).toBe("two-hand-ready");r.update([hand("a"),hand("b")],anchor(),450);expect(r.state).toBe("orb-forming");r.update([hand("a"),hand("b")],anchor(),1400);expect(["orb-stable","orb-charging"]).toContain(r.state);});it("does not release without charge",()=>{const r=new InteractionRuntime();r.update([hand("a"),hand("b")],anchor(),0);r.update([hand("a"),hand("b")],anchor(),500);r.update([hand("a"),hand("b")],anchor(0,2),900);expect(r.state).not.toBe("orb-released");});});

